#cmd obj. bind wrap receiver
#invoker as decorators that execute the commands

class commander(object):
	def __init__(self):

	def invoke(x): x()
	
	@whorf
	def fire: invoke(response)

	@ensen
	def engage: invoke(ensen)
	
	@Data
	def readyRoom: invoke(data)
